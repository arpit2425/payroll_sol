/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PAYROLL_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const FUND_TREASURY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  71, 154, 45, 220, 206, 32, 174, 239,
]);

export function getFundTreasuryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FUND_TREASURY_DISCRIMINATOR,
  );
}

export type FundTreasuryInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountOrg extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrg extends string ? WritableAccount<TAccountOrg> : TAccountOrg,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FundTreasuryInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FundTreasuryInstructionDataArgs = { amount: number | bigint };

export function getFundTreasuryInstructionDataEncoder(): FixedSizeEncoder<FundTreasuryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FUND_TREASURY_DISCRIMINATOR }),
  );
}

export function getFundTreasuryInstructionDataDecoder(): FixedSizeDecoder<FundTreasuryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getFundTreasuryInstructionDataCodec(): FixedSizeCodec<
  FundTreasuryInstructionDataArgs,
  FundTreasuryInstructionData
> {
  return combineCodec(
    getFundTreasuryInstructionDataEncoder(),
    getFundTreasuryInstructionDataDecoder(),
  );
}

export type FundTreasuryInput<
  TAccountOrg extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  org: Address<TAccountOrg>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundTreasuryInstructionDataArgs["amount"];
};

export function getFundTreasuryInstruction<
  TAccountOrg extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: FundTreasuryInput<
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FundTreasuryInstruction<
  TProgramAddress,
  TAccountOrg,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYROLL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    org: { value: input.org ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("org", accounts.org),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getFundTreasuryInstructionDataEncoder().encode(
      args as FundTreasuryInstructionDataArgs,
    ),
    programAddress,
  } as FundTreasuryInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedFundTreasuryInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    org: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: FundTreasuryInstructionData;
};

export function parseFundTreasuryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFundTreasuryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      org: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFundTreasuryInstructionDataDecoder().decode(instruction.data),
  };
}
