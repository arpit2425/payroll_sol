/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findWorkerPda } from "../pdas";
import { PAYROLL_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const ADD_WORKER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  98, 14, 6, 165, 247, 124, 162, 72,
]);

export function getAddWorkerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_WORKER_DISCRIMINATOR);
}

export type AddWorkerInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountOrg extends string | AccountMeta<string> = string,
  TAccountWorker extends string | AccountMeta<string> = string,
  TAccountWorkerPubkey extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrg extends string ? WritableAccount<TAccountOrg> : TAccountOrg,
      TAccountWorker extends string
        ? WritableAccount<TAccountWorker>
        : TAccountWorker,
      TAccountWorkerPubkey extends string
        ? ReadonlyAccount<TAccountWorkerPubkey>
        : TAccountWorkerPubkey,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddWorkerInstructionData = {
  discriminator: ReadonlyUint8Array;
  salary: bigint;
};

export type AddWorkerInstructionDataArgs = { salary: number | bigint };

export function getAddWorkerInstructionDataEncoder(): FixedSizeEncoder<AddWorkerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["salary", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_WORKER_DISCRIMINATOR }),
  );
}

export function getAddWorkerInstructionDataDecoder(): FixedSizeDecoder<AddWorkerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["salary", getU64Decoder()],
  ]);
}

export function getAddWorkerInstructionDataCodec(): FixedSizeCodec<
  AddWorkerInstructionDataArgs,
  AddWorkerInstructionData
> {
  return combineCodec(
    getAddWorkerInstructionDataEncoder(),
    getAddWorkerInstructionDataDecoder(),
  );
}

export type AddWorkerAsyncInput<
  TAccountOrg extends string = string,
  TAccountWorker extends string = string,
  TAccountWorkerPubkey extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  org: Address<TAccountOrg>;
  worker?: Address<TAccountWorker>;
  workerPubkey: Address<TAccountWorkerPubkey>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  salary: AddWorkerInstructionDataArgs["salary"];
};

export async function getAddWorkerInstructionAsync<
  TAccountOrg extends string,
  TAccountWorker extends string,
  TAccountWorkerPubkey extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddWorkerAsyncInput<
    TAccountOrg,
    TAccountWorker,
    TAccountWorkerPubkey,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddWorkerInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountWorker,
    TAccountWorkerPubkey,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYROLL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    org: { value: input.org ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    workerPubkey: { value: input.workerPubkey ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.worker.value) {
    accounts.worker.value = await findWorkerPda({
      org: getAddressFromResolvedInstructionAccount("org", accounts.org.value),
      workerPubkey: getAddressFromResolvedInstructionAccount(
        "workerPubkey",
        accounts.workerPubkey.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("org", accounts.org),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("workerPubkey", accounts.workerPubkey),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddWorkerInstructionDataEncoder().encode(
      args as AddWorkerInstructionDataArgs,
    ),
    programAddress,
  } as AddWorkerInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountWorker,
    TAccountWorkerPubkey,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type AddWorkerInput<
  TAccountOrg extends string = string,
  TAccountWorker extends string = string,
  TAccountWorkerPubkey extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  org: Address<TAccountOrg>;
  worker: Address<TAccountWorker>;
  workerPubkey: Address<TAccountWorkerPubkey>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  salary: AddWorkerInstructionDataArgs["salary"];
};

export function getAddWorkerInstruction<
  TAccountOrg extends string,
  TAccountWorker extends string,
  TAccountWorkerPubkey extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddWorkerInput<
    TAccountOrg,
    TAccountWorker,
    TAccountWorkerPubkey,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddWorkerInstruction<
  TProgramAddress,
  TAccountOrg,
  TAccountWorker,
  TAccountWorkerPubkey,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYROLL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    org: { value: input.org ?? null, isWritable: true },
    worker: { value: input.worker ?? null, isWritable: true },
    workerPubkey: { value: input.workerPubkey ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("org", accounts.org),
      getAccountMeta("worker", accounts.worker),
      getAccountMeta("workerPubkey", accounts.workerPubkey),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddWorkerInstructionDataEncoder().encode(
      args as AddWorkerInstructionDataArgs,
    ),
    programAddress,
  } as AddWorkerInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountWorker,
    TAccountWorkerPubkey,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedAddWorkerInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    org: TAccountMetas[0];
    worker: TAccountMetas[1];
    workerPubkey: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: AddWorkerInstructionData;
};

export function parseAddWorkerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddWorkerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      org: getNextAccount(),
      worker: getNextAccount(),
      workerPubkey: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddWorkerInstructionDataDecoder().decode(instruction.data),
  };
}
