/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findOrgPda } from "../pdas";
import { PAYROLL_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CREATE_ORG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  48, 115, 187, 249, 36, 3, 186, 175,
]);

export function getCreateOrgDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_ORG_DISCRIMINATOR);
}

export type CreateOrgInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountOrg extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrg extends string ? WritableAccount<TAccountOrg> : TAccountOrg,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateOrgInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type CreateOrgInstructionDataArgs = { name: string };

export function getCreateOrgInstructionDataEncoder(): Encoder<CreateOrgInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_ORG_DISCRIMINATOR }),
  );
}

export function getCreateOrgInstructionDataDecoder(): Decoder<CreateOrgInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateOrgInstructionDataCodec(): Codec<
  CreateOrgInstructionDataArgs,
  CreateOrgInstructionData
> {
  return combineCodec(
    getCreateOrgInstructionDataEncoder(),
    getCreateOrgInstructionDataDecoder(),
  );
}

export type CreateOrgAsyncInput<
  TAccountOrg extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  org?: Address<TAccountOrg>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateOrgInstructionDataArgs["name"];
};

export async function getCreateOrgInstructionAsync<
  TAccountOrg extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CreateOrgAsyncInput<
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateOrgInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYROLL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    org: { value: input.org ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.org.value) {
    accounts.org.value = await findOrgPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
      name: getNonNullResolvedInstructionInput("name", args.name),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("org", accounts.org),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateOrgInstructionDataEncoder().encode(
      args as CreateOrgInstructionDataArgs,
    ),
    programAddress,
  } as CreateOrgInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type CreateOrgInput<
  TAccountOrg extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  org: Address<TAccountOrg>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateOrgInstructionDataArgs["name"];
};

export function getCreateOrgInstruction<
  TAccountOrg extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CreateOrgInput<TAccountOrg, TAccountAuthority, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): CreateOrgInstruction<
  TProgramAddress,
  TAccountOrg,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYROLL_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    org: { value: input.org ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("org", accounts.org),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateOrgInstructionDataEncoder().encode(
      args as CreateOrgInstructionDataArgs,
    ),
    programAddress,
  } as CreateOrgInstruction<
    TProgramAddress,
    TAccountOrg,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedCreateOrgInstruction<
  TProgram extends string = typeof PAYROLL_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    org: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateOrgInstructionData;
};

export function parseCreateOrgInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateOrgInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      org: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateOrgInstructionDataDecoder().decode(instruction.data),
  };
}
